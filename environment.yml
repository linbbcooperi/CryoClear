name: cryotriage
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=2.0
  - scikit-image>=0.21
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - opencv
  - pytest
  - pip
  - pip:
      - mrcfile>=1.4
      - starfile>=0.5
      - streamlit>=1.30
      # On the venue workstation, also install (matched to CUDA):
      #   torch  (https://pytorch.org)
      #   topaz-em
      #   aspire
